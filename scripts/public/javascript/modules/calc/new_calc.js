export default class calc {
    constructor (title, subtitle, link, result, labels, inputs) {
        this.title = title
        this.subtitle = subtitle
        this.link = link
        this.result = result
        this.labels = labels
        this.inputs = inputs
    }
}